services:
  - type: web
    name: absensi-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    healthCheckPath: /api/health

databases:
  - name: absensi-db
    databaseName: absensi_ai
    user: absensi
